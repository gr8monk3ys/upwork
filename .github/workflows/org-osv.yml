name: org-osv

on:
  workflow_dispatch:
  schedule:
    - cron: "23 9 14 * *"  # monthly (minute-safe policy)

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: org-osv-${{ github.ref }}
  cancel-in-progress: true

jobs:
  osv:
    uses: gr8monk3ys/github/.github/workflows/reusable-osv.yml@5848461992e5a86bbc16712ee06ee3e6b64a2947
