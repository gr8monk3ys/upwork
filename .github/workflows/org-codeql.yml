name: org-codeql

on:
  workflow_dispatch:
  schedule:
    - cron: "23 9 14 * *"  # monthly (minute-safe policy)

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  codeql:
    uses: gr8monk3ys/github/.github/workflows/reusable-codeql.yml@cf21251fb08ecd80847f10a060b3599a7c518d4b
    with:
      languages: python
      upload: never
